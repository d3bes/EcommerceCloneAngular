import { SearchPrductPipe } from './search-prduct.pipe';

describe('SearchPrductPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPrductPipe();
    expect(pipe).toBeTruthy();
  });
});
